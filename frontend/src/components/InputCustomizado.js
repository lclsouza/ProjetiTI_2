import React from 'react';

export default ({ id, type, className, name, placeholder, value, onChange }) => (

    <input id={id} type={type} className={className} name={name} placeholder={placeholder} value={value} onChange={onChange} />

)


